# Build outputs
/brouter
/cmd/brouter/brouter
dist/
result

# macOS handler bundle build output
dist/

# Go test and coverage artifacts
*.test
*.out
coverage/
*.prof

# Local agent reports and scratch space
/.tmp/

# Watcher control socket (runtime artifact)
.watch.sock

# The watcher config is versioned guardrails (TASK-0004); re-include it
# because global gitignores may hide it.
!.watch.yaml

# Editors
.idea/
.vscode/
*.swp
